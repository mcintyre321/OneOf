using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace OneOf {
    internal static class Functions {
        internal static string FormatValue<T>(Type type, T value) => $"{type.FullName}: {value?.ToString()}";
    }
}
